---
title: "Got Dill? — App Store Readiness"
subtitle: "What Apple requires, what we require, and where each stands"
format:
  html:
    toc: true
    toc-depth: 2
---

Status as of **2026-09-04**. This tracks the path from a two-person TestFlight
beta to something a stranger can install. Update the status column as things
move; do not delete rows, so the history of what was decided stays readable.

**Legend** — ✅ done and verified · 🟡 done but unverified · ⬜ not started ·
❓ unknown, needs a look · 🚫 blocked on a decision

---

## What to do next, in order

Re-ranked 2026-09-04. The ordering principle: **one item has a two-week lead
time and everything else is work you can do the day you decide to.** That one
goes first regardless of how it ranks on urgency.

| # | Item | Why here | Size |
|---|---|---|---|
| 1 | ~~Instrument the AI calls~~ **done 2026-09-04, undeployed** | `amplify/data/telemetry.ts` + five handlers converted to structured JSON events; `logFormat: JSON` set in `backend.ts`. Token usage now emitted on both `inferProductAisle` paths. The clock starts when this deploys, not when it was written | ✅ |
| 2 | **Persist a trip** — a row written when a session ends | The meter unit does not exist as data. Needed whatever the price turns out to be | ~half a day |
| 3 | **Cut 1.8.0 to TestFlight** | 16 commits of fixes are sitting on `main` and neither phone's build is what testers would get. Testers cannot test what has not shipped | minutes |
| 4 | **Household lifecycle testing** | The only outstanding item that can *destroy data*. `deleteHouseholdData` has never been run through account deletion with a shared household. Everything else on this list costs money or time; this one costs somebody's list | a session, needs throwaway accounts |
| 5 | **Decide: TestFlight, unlisted, or public** | Determines whether rows 6–8 are required at all. It is a decision, not work | — |
| 6 | Privacy nutrition labels, age rating, demo account | Only if past TestFlight. Wrong labels are a rejection | a session |
| 7 | Screenshots, description, keywords | Only if a public listing | a session |
| 8 | Legal entity | Blocks the ToS and the seller name. A decision, not work | — |
| 9 | **Claude → Bedrock** (see *Moving the AI inside AWS*) | Not now, but standing near the top: it deletes `ANTHROPIC_API_KEY` entirely and it touches the same seven call sites as the Haiku move, so the two want to be one edit | ~half a day |
| 10 | S3 per-household scoping | Real, but the payoff for exploiting it is somebody's grocery photo | ~a day |
| 11 | `joinHousehold` rate limiting, signup gating | Deferred on proportionality. Fold in when the file is already open | — |

Moving `inferProductAisle` to Haiku is not on this list because it should be
*measured* before it is moved — that is what item 1 is for. When it is moved, do
it as part of item 9: both changes rewrite the same model-id lines in the same
seven handlers, and doing them separately means reviewing that blast radius
twice.

---

## The decision everything else hangs on

**How are we distributing?** Still open, and it changes which rows below matter.

| Option | Cap | Review | Longevity |
|---|---|---|---|
| TestFlight external | 10,000 testers | Beta App Review | Builds expire every 90 days |
| Unlisted App Distribution | none | Full App Store review | Permanent, no discovery |
| Public listing | none | Full App Store review | Permanent |

If we stay on TestFlight, most of *Store listing* below is not required yet.
Everything in *Apple hard requirements* and *Our own bar* applies either way.

You cannot cap downloads on a normal listing. Any "limited release" has to come
from TestFlight's own cap or from being unlisted.

---

## Apple hard requirements

These are rejections, not preferences.

| Item | Status | Notes |
|---|---|---|
| In-app account deletion (5.1.1(v)) | ✅ | Built and verified 2026-08-31 against production: Cognito user gone, `User` row gone, household and its 245 items untouched, no orphaned group. The shared-household branch — detach, group removal, remaining count — is still untested |
| Account deletion documented publicly | ✅ | Live at <https://got-dill.com/account-deletion>. Source `docs/account-deletion.html`; the in-app path it describes (Settings → Danger Zone → Delete my account) was checked against `SettingsView` |
| Privacy policy amendment for logging | 🚫 | **Deferred deliberately 2026-09-04.** The published policy says server logs "do not record the contents of your list"; that was false — `[PARSE] result`, `[PARSE] claude raw response`, `[INFER] Received request` and `joinHousehold`'s full-event dump all wrote user content, verified in production CloudWatch. The structured-logging pass fixes it **going forward**. Not amending the text yet because the logged fields are still expanding; a policy written now gets rewritten. **Two things must both land before this is closed:** the retention decision below (which clears the historical backlog), and a final field list |
| Log retention | ✅ | **90 days, applied 2026-09-04** to all 17 `/aws/lambda/amplify-d2rsreno8nimo5-ma-*` groups (was `None` on every one; ~1.75 MB total). Verified 17/17. CloudWatch purges anything older asynchronously, which clears the grocery-list contents dating to May. Tighten if volume grows |
| Privacy policy URL | ✅ | **Live at <https://got-dill.com/privacy>**, linked in-app under Create Account and in Settings (`AppIdentity.privacyPolicyURL`). Source is `docs/privacy.html`, written 2026-09-03 against verified behaviour. Not lawyer-reviewed, and says so on the page. `website/privacy-policy.md` was the older draft and was deleted 2026-09-03; `docs/privacy.html` is the single source |
| Privacy nutrition labels | ⬜ | Must declare email, audio, photos, and the Anthropic/OpenAI processing. Wrong answers here are a rejection |
| Demo account for review (2.1) | ⬜ | Reviewers need a working account **and** a household with data in it. A fresh account sees an empty app |
| Age rating questionnaire | ⬜ | |
| Export compliance declaration | ✅ | Verified 2026-09-03: `INFOPLIST_KEY_ITSAppUsesNonExemptEncryption = NO` in both Debug and Release. Accurate — the app uses standard HTTPS/TLS and no custom cryptography |
| Support URL | ✅ | Live at <https://got-dill.com/support>. Source `docs/support.html`. Support address is mike@byteclub.com — swap it if a got-dill.com mailbox ever exists |

---

## Our own bar before strangers get in

Not Apple's rules — ours. See `world-release-blockers` in session memory.

| Item | Status | Notes |
|---|---|---|
| Per-household authorization | ✅ | Verified 2026-08-30 with a real outsider account: reads and writes both denied |
| Invite codes single-use, 10 min, 8 chars | ✅ | Tightened 2026-09-03 and deployed (Amplify job on `71cafb1`). Was 30 min / 6 chars. `regenerateInviteCodeFunction` was still generating with `Math.random()` — the live "Generate New Code" path — now `randomInt` from node:crypto. QR scanning added for in-person invites |
| Household required for every AI call | ✅ | Reject and allow paths both verified |
| AppSync request logging | ✅ | ERROR level, bodies excluded, 14-day retention; verified by generating denials |
| S3 enumeration closed | ✅ | `list` withheld; deployed policy confirmed |
| S3 per-household scoping | ⬜ | Unchanged. Anyone holding a key can still read or delete that file. Lambda broker is the fix, ~a day. See `s3-scoping-plan` |
| `inferProductAisle` store ownership | ✅ | Fixed 2026-09-03. The store read now projects `householdId` and rejects unless it is one of the caller's Cognito groups. The mappings read was also made serial after it — running them together meant another household's mappings were read before the check could reject, even though nothing reached the caller |
| `joinHousehold` rate limiting | 🚫 | Still deferred, and **less urgent than this file used to claim**. With 8-character single-use 10-minute codes, guessing is not the concern; burning Lambda invocations is, and a WAF rate rule is the answer to that, not a counter. The error-message half is done: invalid / expired / already-used now return one identical refusal, so a guesser is no longer told when they found a real code. Verified in the deployed bundle 2026-09-04 — one identical refusal at all three call sites |
| Password floor above 6 chars | ✅ | Raised to 8 with lowercase + number in `backend.ts`, deployed and **verified against the live pool 2026-09-04**: `MinimumLength: 8, RequireLowercase: true, RequireNumbers: true, RequireUppercase: false, RequireSymbols: false`. Existing passwords keep working — Cognito validates on set |
| Signup gating | ⬜ | Much less urgent now — a junk account costs a Cognito row and nothing else |

---

## To do: household lifecycle testing

A whole class of behaviour has never been exercised, because doing it properly
needs more than one throwaway account and a household that is safe to destroy.

**Set up a separate test household with two or three accounts.** Gmail
plus-addressing gives as many as needed — `mike.akram+t1@gmail.com`,
`+t2`, `+t3` — and codes land in the normal inbox. One creates a household, the
others join it with invite codes. Put a few items, a store and an aisle layout in
it so there is something to observe being kept or destroyed.

**Never point any of this at `TooFat&TooCurious`.** Everything below is
destructive, and there is one environment.

### Paths that have never been run

| Path | What it exercises | Verify |
|---|---|---|
| Member deletes their account, others remain | `detachUser`, group revoke, remaining count | Their items stay on the list; their Cognito group membership is gone; the household is intact; the other members can still read everything |
| **Last** member deletes their account | `deleteHouseholdData` cascade | Household, items, commits, stores, mappings and the Cognito group are all gone. Nothing orphaned |
| Owner removes a member | `remove`, which is owner-only | The removed person loses access immediately, not when their token expires |
| Owner leaves, others remain | The household with no owner | Remaining members can still use it; nobody can be removed any more, by design |
| Member leaves and rejoins | Colour reassignment, group re-grant | They do not collide with a colour someone took while they were away |
| Two people join on one code, racing | `consumeInviteCode` conditional write | Exactly one gets in; the other is told the code was used |

### Why it matters more than it looks

`deleteHouseholdData` is the most destructive code in the app and it is reached
by two different actions. It has only ever been observed through `leave`. The
reasoning "the account-deletion path reuses code that already works" is precisely
the reasoning that broke joining on 2026-08-31 — the code was fine, the IAM grant
around it was not.

The account-deletion path was verified on 2026-08-31 only for a user with **no**
household. That is the least interesting of the three branches.

## Store listing

Only needed if we go past TestFlight.

| Item | Status | Notes |
|---|---|---|
| App name | ✅ | **Got Dill?** — the question mark is part of the name. Chosen to clear the OURGROCERIES trademark |
| Bundle ID / SKU | ✅ | `com.byteclub.grocery.app` / `notourgroceries`. Both locked by Apple forever; internal-only, never shown |
| App icon | ✅ | |
| Screenshots | ⬜ | Needed per device size |
| Description, keywords, subtitle | ⬜ | |
| Marketing URL | ⬜ | Optional |

---

## Website

Drafted 2026-08-31, parked. See `website/README.md` for the full placeholder list.

| Item | Status |
|---|---|
| Privacy policy | ✅ live, single source at `docs/privacy.html` |
| Terms of service | 🚫 blocked | Needs `[LEGAL ENTITY]` and `[JURISDICTION]`, which are decisions, not text. Not an Apple requirement for a free app with no purchases — only the privacy policy is |
| Support page | ✅ live |
| Account deletion page | ✅ live |
| Domain | ✅ `got-dill.com`, registered 2026-09-03, GoDaddy, 1 yr + Full Domain Protection, renews 2027-09 at \$37.98. DNS delegated to Route 53 zone `Z0678500J7Q7Z5FMLMYC` |
| Hosting | ✅ S3 `got-dill-com-site` (private, OAC-only) behind CloudFront `E12UV51UT41FNP`, ACM cert for apex + www, HTTP→HTTPS, TLS 1.2+. ~\$0.50/mo. To update: `aws s3 cp` then invalidate |

**Open questions carried from the drafts:** effective date, support email, legal
entity, jurisdiction, retention period after deletion (including AWS PITR
windows), and whether a lawyer reviews before public release.

---

## Legal

| Item | Status | Notes |
|---|---|---|
| Trademark clearance | ✅ | Renamed off `NotOurGroceries`; OURGROCERIES is a live mark covering the same goods |
| CCPA / CPRA | ❓ | Almost certainly below all three thresholds — revenue, 100k California consumers, 50% of revenue from selling data. Revisit before wide release. Not a lawyer |
| GDPR | ❓ | Plausible the moment there is one EU or UK user. No compliance claim written either way |
| Legal entity | ⬜ | Individual or company; drives the seller name and the liability wording |

---

## Release mechanics

| Item | Status | Notes |
|---|---|---|
| `scripts/release.sh` | ✅ | Tags `vX.Y.Z`, bumps `main` to the next minor |
| TestFlight CI on tag push | ✅ | `.github/workflows/testflight.yml`, stamps a datetime build number |
| 90-day expiry job | ✅ | Scheduled rebuild so TestFlight builds do not lapse |
| CHANGELOG up to date | ✅ | `[Unreleased]` written 2026-09-03 covering all 15 commits since v1.7.0. Ready for `./scripts/release.sh` whenever 1.8.0 is cut |
| TestFlight build current | 🟡 | v1.7.0 shipped 2026-08-31 and is current *with itself*, but 15 commits behind `main`. `main` carries 1.8.0, unreleased. Both phones run local builds well ahead of TestFlight |

---

## Moving the AI inside AWS

Worth exploring before public release. The motive is the privacy story and the
API keys, not cost.

**Where the calls are today:** seven Lambdas use Anthropic's SDK directly
(`parseIngredients`, `inferProductAisle`, `aisleExtractionJobHandler`,
`matchProductsToAisles`, `extractAislesFromImages`, `extractAisleMappings`,
`generateFullMappings`). Exactly one uses OpenAI, and it does nothing but
transcription.

### Claude → Bedrock: straightforward

| | |
|---|---|
| Effort | ~half a day. `@anthropic-ai/bedrock-sdk` keeps the same message shape, so the seven sites are largely an import and a model id |
| Wins | `ANTHROPIC_API_KEY` disappears entirely — Bedrock authenticates with IAM, so there is no long-lived secret in SSM to leak or rotate. One vendor, one bill, same region |
| Check first | Both `claude-haiku-4-5` and `claude-sonnet-4-6` appear in the Bedrock model catalogue as of 2026-09-04. That is the catalogue, not your account — confirm they are enabled for `mine` in us-east-1 before planning around it |
| Watch | Bedrock per-token pricing differs, and the difference is the cross-region inference profile. Direct `claude-haiku-4-5` is \$1.00/\$5.00 per Mtok; plain `anthropic.claude-haiku-4-5` on Bedrock matches it, but `us.anthropic.claude-haiku-4-5` — the profile a us-east-1 Lambda would normally use — is \$1.10/\$5.50, a **10% premium**. Same 10% on Sonnet. At the volumes in *Monetization* that is cents, but it is a real markup and it goes the wrong way |
| Watch | Prompt caching exists with different mechanics. `parseIngredients` is the one function where this matters — its cache is currently doing most of the work (44k cache-write against 4.5k uncached over 30 days). Do not assume the caching carries over unexamined |

### Whisper → somewhere in AWS: the harder half

Bedrock does not host Whisper. The options are Amazon Transcribe, or running a
speech model ourselves on SageMaker or ECS.

Neither is a swap. The current setup is tuned to Whisper's specific failure
modes: `gpt-4o-mini-transcribe` was chosen over `whisper-1` because the latter
completes trailing silence with YouTube caption boilerplate, and
`stripHallucinatedTail` exists to catch what still gets through. A different
engine has different failure modes and needs its own evaluation against real
dictation, not fixtures.

**How much is at stake, measured from CloudWatch on 2026-08-31:**

| | |
|---|---|
| Successful transcriptions, all time | 16 |
| Median transcript | 206 characters |
| Median latency | 3.2 s |
| Cost | Pennies. Not a reason to move |

So this is a disclosure decision, not an economic one. Moving Claude alone takes
the privacy policy from two external processors to one; the "AWS only" story
needs both, and transcription is the half with quality risk attached.

### Suggested order

1. Move Claude to Bedrock. Real security win, low risk, independent of everything
   else. **Do it in the same pass as the `inferProductAisle` model change** — both
   rewrite the model id in the same seven handlers, so one edit and one review
   instead of two. That ordering makes item 1 of the ranked list a prerequisite:
   measure first, then move the model and the vendor together.
2. Evaluate Amazon Transcribe against real dictation separately, with the
   hallucination behaviour as the thing to measure. Only then decide whether the
   single-processor story is worth the change.

---

## Monetization: what the meter costs

Measured 2026-09-04 from CloudWatch over the preceding 30 days, one household,
two people. This is the math that was the standing prerequisite.

### Volume

| Function | Model | Invocations / 30 d |
|---|---|---|
| `inferProductAisle` | `claude-sonnet-4-6` | **49** |
| `parseIngredients` | `claude-haiku-4-5` | 29 |
| `transcribeAudio` | `gpt-4o-mini-transcribe` | 21 |

### Cost, and how firmly it is known

`parseIngredients` logs input tokens, so its input cost is exact: over the 14
parses that logged usage — 44,018 cache-write, 17,158 cache-read, 4,469 uncached
— **\$0.061**. Output is not logged; its ceiling is `max_tokens` 1024 per call.

| | Known | Ceiling |
|---|---|---|
| `parseIngredients`, 29 calls | \$0.13 | **\$0.28** |
| `inferProductAisle`, 49 calls | *nothing logged* | **\$6.02** |
| `transcribeAudio`, 21 calls | *only bytes and characters* | pennies |
| AWS, whole account | \$3.19 | — |

The \$6.02 is `max_tokens` 8192 × 49 × Sonnet output — a true ceiling nobody
will reach, not a forecast. A plausible middle (3k in, 1k out) is **\$1.18**.
The honest statement is that one household costs somewhere between \$1 and \$6
a month in Claude, and the width of that range is entirely one function's
missing log line.

### The two findings that matter more than the range

**1. The expensive call is on the expensive model.** `inferProductAisle` is the
most-called AI function and the only one on Sonnet, with the largest
`max_tokens` of the three. It is aisle-guessing — short, structured, repetitive
output against a fixed store layout. The same plausible middle on
`claude-haiku-4-5` is **\$0.39 instead of \$1.18**. Moving it is a bigger lever
on the bill than any price point, and it should be measured before it is priced.

**2. A shopping trip is not recorded anywhere.** The chosen meter unit does not
exist as data. `Household` carries `shoppingStatus`, `activeShopperId`,
`shoppingStoreId` and `shoppingStartedAt` — all *current-state* fields,
overwritten by the next trip. `CommitAction` has eight values and none of them
is starting or finishing a session. So a completed trip leaves no row, there is
no history to count, and "two trips a month" cannot be enforced or even reported
until trips are persisted.

### What unblocks pricing

1. **Log `response.usage`** in `inferProductAisleFunction` and add output tokens
   to `parseIngredients`. Three lines each. A fortnight later the range above
   collapses to a number.
2. **Persist a trip.** A row written when a session ends, carrying householdId,
   store, item count and duration. Needed for the meter regardless of price, and
   it is what makes cost-per-trip reportable rather than inferred.
3. **Then decide.** With a real cost per trip and a real trips-per-month
   distribution, the free cap and the \$0.99 top-up stop being guesses.

Read the Anthropic and OpenAI billing dashboards in the meantime — that gives
today's actual spend immediately, though not the per-call breakdown that tells
you which function to move.

## What is deliberately not being built

- **Monetization.** Direction agreed — no feature gating, volume metered on
  trips. **No longer blocked on unit cost**; see *Monetization: what the meter
  costs* above. It is now blocked on two build items named there.
- **Ads.** Rejected. Sessions are seconds long and it is a shared family utility.
